/**:
  ros__parameters:
    mode: prop_m

    safety:
      tether_enabled: true
      max_tether_radius_m: 15.0

    limits:
      v_max_mps:
        prop_m: 0.05
      omega_max_rps:
        prop_m: 0.30
